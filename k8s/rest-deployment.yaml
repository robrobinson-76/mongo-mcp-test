apiVersion: apps/v1
kind: Deployment
metadata:
  name: bank-ods-rest
  namespace: bank-ods
  labels:
    app: bank-ods-rest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bank-ods-rest
  template:
    metadata:
      labels:
        app: bank-ods-rest
    spec:
      containers:
        - name: rest
          image: bank-ods-rest:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: bank-ods-config
            - secretRef:
                name: bank-ods-secrets
          resources:
            requests:
              cpu: 125m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 2
