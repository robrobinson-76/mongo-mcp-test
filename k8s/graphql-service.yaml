apiVersion: v1
kind: Service
metadata:
  name: bank-ods-graphql
  namespace: bank-ods
spec:
  selector:
    app: bank-ods-graphql
  ports:
    - port: 80
      targetPort: 8001
      protocol: TCP
  type: ClusterIP
